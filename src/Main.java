public class Main {
    public static void main(String [] args){


        /*
        // find a center of mass of the locations of the attacks

        Point a = new Point(0,0);
        Point b = new Point (5,5);
        Point [] points = {a,b};

        double xSum = 0;
        double ySum = 0;
        for(int i = 0; i < 2; i++){
            xSum += points[i].getX();
            ySum += points[i].getY();
        }

        double xAvg = xSum / points.length;
        double yAvg = ySum / points.length;

        //System.out.println("Distance: " + a.distance(b) );
        System.out.println("xAvg = " + xAvg);
        System.out.println("yAvg = " + yAvg);



        // Center of mass aka average x and average y



        // get long lat


        // plug long lat back into maps
        *
        *
        */

        /*
        String s = "[1,2]";
        String temp = s.substring(1,s.length()-1);
        System.out.println("No []: " + temp);
        String [] temp2 = temp.split(",");
        for(int i = 0; i < temp2.length; i++){
            System.out.println(temp2[i]);
        }
*/


    }
}
